version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: rabobank-postgres
    environment:
      POSTGRES_DB: rabobank_banking
      POSTGRES_USER: rabobank_user
      POSTGRES_PASSWORD: rabobank_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rabobank_user -d rabobank_banking"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: .
    container_name: rabobank-backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/rabobank_banking
      DB_USERNAME: rabobank_user
      DB_PASSWORD: rabobank_pass
      SPRING_PROFILES_ACTIVE: docker 
      LOGGING_LEVEL_ORG_FLYWAYDB: DEBUG
     
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:
